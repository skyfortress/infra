apiVersion: v1
kind: Namespace
metadata:
  name: imggen
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: imggen-deployment
  namespace: imggen
spec:
  replicas: 1
  selector:
    matchLabels:
      app: imggen-server
  template:
    metadata:
      labels:
        app: imggen-server
    spec:
      nodeSelector:
        homelab: "true"
      containers:
        - name: imggen-pod
          image: docker.io/skyfortress/generator:latest
          envFrom:
            - secretRef:
                name: generator-secrets
          ports:
            - containerPort: 3000
              protocol: TCP
        - name: imggen-api-pod
          image: docker.io/skyfortress/generator:latest
          env:
            - name: GENERATION_LIMIT
              value: "50"
            - name: FRONTEND_URL
              value: "https://skyfortress.dev"
            - name: GOOGLE_CALLBACK_URL
              value: "https://skyfortress.dev/api/auth/google/callback"
          envFrom:
            - secretRef:
                name: generator-api-secrets
          ports:
            - containerPort: 4000
              protocol: TCP
          volumeMounts:
            - name: images-volume
              mountPath: /usr/src/web/images
      volumes:
        - name: images-volume
          persistentVolumeClaim:
            claimName: imggen-images-pvc-new
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nsfw-detector-deployment
  namespace: imggen
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nsfw-detector
  template:
    metadata:
      labels:
        app: nsfw-detector
    spec:
      nodeSelector:
        homelab: "true"
      containers:
        - name: nsfw-detector-pod
          image: docker.io/skyfortress/generator:latest
          ports:
            - containerPort: 8000
              protocol: TCP
          resources:
            limits:
              nvidia.com/gpu: 1
            requests:
              nvidia.com/gpu: 1
---
apiVersion: v1
kind: Service
metadata:
  name: nsfw-detector-service
  namespace: imggen
spec:
  selector:
    app: nsfw-detector
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: imggen-server
  namespace: imggen
spec:
  selector:
    app: imggen-server
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
    - name: api
      protocol: TCP
      port: 4000
      targetPort: 4000
  type: LoadBalancer
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: imggen-server-ingress
  namespace: imggen
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: letsencrypt-cloudflare
spec:
  ingressClassName: nginx
  rules:
    - host: skyfortress.dev
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: imggen-server
                port:
                  number: 4000
          - path: /
            pathType: Prefix
            backend:
              service:
                name: imggen-server
                port:
                  number: 3000
    - host: generator.skyfortress.dev
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: imggen-server
                port:
                  number: 4000
          - path: /
            pathType: Prefix
            backend:
              service:
                name: imggen-server
                port:
                  number: 3000
  tls:
    - hosts:
        - skyfortress.dev
        - generator.skyfortress.dev
        - unrestricted.skyfortress.dev
      secretName: letsencrypt-cloudflare-key
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: imggen-images-pvc-new
  namespace: imggen
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
